import { mount } from '@vue/test-utils';
import HomeFeed from "@/views/HomeFeed.vue";

// Example file for vitest

/*describe('HomeFeed.vue', () => {
    it('renders title and list of items', () => {
        const title = 'Home Feed';
        const items = [
            { id: 1, content: 'First content' },
            { id: 2, content: 'Second content' },
        ];

        const wrapper = mount(HomeFeed, {
            props: { title, items },
        });
        
        expect(wrapper.find('h1').text()).toBe(title);
        
        const listItems = wrapper.findAll('li');
        expect(listItems).toHaveLength(items.length);
        expect(listItems[0].text()).toBe(items[0].content);
        expect(listItems[1].text()).toBe(items[1].content);
    });
});*/
